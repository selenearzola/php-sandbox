<?php

//  Because we're not using a database we create constants for  $username and $password.
//  For create a constant in PHP we use the word define. Also a constant it's a value that never change.

define(USERNAME, 'Candida Clementina');
define(PASSWORD, '1234');

// Config file it's used also for database credentials.